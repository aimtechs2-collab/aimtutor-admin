"use client";

import ManageCoursesClient from "./ManageCoursesClient";

export default function AdminCatalogPage() {
  // Aim_Admin_Portal reference: `src/components2/ManageCourses.jsx`
  // This route now renders the same "Master Categories" UI instead of the simplified catalog list.
  return <ManageCoursesClient />;
}
